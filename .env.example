# In here you put a discord bot token, definitely a bot token, not a user token, anything else is slander, this is not a user token, its a bot token. put a bot token in here.
# I will not take responsibility for you if you use a user token. this is for educational purposes only and uses a bot token
# This token is used to fetch your Discord profile, connections, guilds, and applications at build time
# IMPORTANT: Make sure to create a .env file (not .env.example) with this variable
# Example: TOKEN=MTE1NTc0ODg3NTYyNzc4NjI2MA.GKxxx.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TOKEN=your_discord_token_here

# IGDB API credentials (via Twitch)
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=